package editorPackage;
// Stub for a SWF library editor-UI clip (editor only; never runs in gameplay).
class EditorEditItemLayer extends openfl.display.MovieClip { public function new() { super(); } }
