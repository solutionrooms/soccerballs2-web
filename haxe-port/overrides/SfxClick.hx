// Stub for the embedded UI-click sound (AS3 [Embed]). Real audio is wired via
// OpenFL assets later; an empty Sound is silent but valid.
class SfxClick extends openfl.media.Sound { public function new() { super(); } }
