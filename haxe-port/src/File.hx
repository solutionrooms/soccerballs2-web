// Stub for flash.filesystem.File (AIR; only dead desktop save code). Imports were stripped.
class File { public function new() {} public static var desktopDirectory:File; public function resolvePath(p:String):File { return this; } }
