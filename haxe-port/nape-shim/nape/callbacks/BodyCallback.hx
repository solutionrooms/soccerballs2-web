package nape.callbacks;

import nape.phys.Body;

/** nape.callbacks.BodyCallback shim — imported but unused; minimal stub. */
class BodyCallback {
	public var body(default, null):Body;

	public function new() {}
}
