package com.adobe.images;
// Stub. Used only by the (disabled) walkthrough recorder for screenshots.
import openfl.display.BitmapData;
import openfl.utils.ByteArray;
class PNGEncoder {
	public static function encode(img:BitmapData):ByteArray { return new ByteArray(); }
}
